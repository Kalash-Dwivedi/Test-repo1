# Repo created in VS
## Commands
cd=change directory in a file\
cd..=get out of that folder\
mkdir=create new folder in it

git add filename.type
git add . (for all)
git commit -m "what's next"\
git commit -m "what's next" -m "description"
git push\
origin main may require\
-u can avoid it